const steps=[...document.querySelectorAll(".step")],panels=[...document.querySelectorAll(".form-step")],dots=[...document.querySelectorAll(".dot")],nextButton=document.querySelector(".next-button");let currentStep=0;function setStep(e){currentStep=Math.max(0,Math.min(e,steps.length-1)),steps.forEach((t,s)=>t.classList.toggle("active",s===currentStep)),panels.forEach((t,s)=>t.classList.toggle("active",s===currentStep)),dots.forEach((t,s)=>t.classList.toggle("active",s===currentStep)),nextButton.textContent=currentStep===steps.length-1?"Finish":"Next step"}steps.forEach(e=>{e.addEventListener("click",()=>setStep(Number(e.dataset.step)))}),nextButton.addEventListener("click",()=>{currentStep<steps.length-1?setStep(currentStep+1):(nextButton.textContent="Done",nextButton.disabled=!0)});
